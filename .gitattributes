# 조립 산출물 — 충돌 시 tools/sync.py 가 재생성으로 해결
js/holeimgdb.js -diff

# 이미지·엑셀은 바이너리로 취급 (줄바꿈 변환 금지)
*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.xlsx binary
*.mp4 binary

# 텍스트는 저장소에 LF로 통일 (집·회사 줄바꿈 차이로 인한 가짜 충돌 방지)
*.py text eol=lf
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md text eol=lf
