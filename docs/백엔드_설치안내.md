# 골프라이프 백엔드 설치 안내 (약 5분, 무료)

이걸 설치하면 두 가지가 켜집니다:
1. **관리자 통계** — 접속수·인기 골프장·기능 사용·연령/성별 분포 (좌표는 절대 수집 안 함)
2. **맛집 실제 사진** — 카카오맵에 등록된 '그 가게' 사진이 앱 안에 바로 표시

## 설치 순서 (사장님이 직접, 구글 계정 필요)

1. https://sheets.new 접속 → 새 스프레드시트가 열림 → 이름을 `골프라이프 통계`로 변경
2. 메뉴 **확장 프로그램 → Apps Script** 클릭
3. 열린 편집기에서 기본 코드를 모두 지우고,
   `tools/apps_script/Code.gs` 파일 내용을 **전체 복사해 붙여넣기**
4. 코드 맨 위 `ADMIN_PW = "golf2026!"` 를 **원하는 비밀번호로 변경** → 저장(💾)
5. 오른쪽 위 **배포 → 새 배포** 클릭
   - 유형 선택(⚙️): **웹 앱**
   - 설명: 아무거나 (예: v1)
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자** ← 중요!
   - **배포** 클릭 → 권한 요청이 뜨면 **허용** (본인 계정이라 안전)
6. 나오는 **웹 앱 URL** (`https://script.google.com/macros/s/…/exec`)을 복사해서
   **클로드에게 전달** — 앱에 연결해 드립니다

## 이후 관리

- 통계 원본: 방금 만든 스프레드시트의 `log` 시트에 자동 기록
- 베타 의견 원본: 같은 스프레드시트의 `fb` 시트 (2026-07-31 추가)
- 관리자 화면: 앱 주소 뒤에 **`/admin.html`** + 위에서 정한 비밀번호
- 코드를 수정했을 때: 배포 → 배포 관리 → ✏️ → 버전 '새 버전' → 배포
  (URL은 그대로 유지됩니다)

## 관리자 비밀번호를 코드 밖으로 (권장)

코드에 적힌 비밀번호는 저장소를 공개로 돌리는 순간 함께 노출됩니다.
Apps Script 편집기 → **프로젝트 설정(⚙) → 스크립트 속성 → 속성 추가**
- 속성 이름: `ADMIN_PW`
- 값: 원하는 비밀번호

넣지 않으면 코드에 적힌 기본값으로 그대로 동작하므로, 급하지 않으면 나중에 해도 됩니다.

## 베타 의견 알림 메일 (2026-07-31 추가)

이용자가 앱에서 의견을 보내면 **brown.rigoon@gmail.com** 으로 즉시 메일이 옵니다.
- 주소를 바꾸려면 `Code.gs` 의 `ADMIN_MAIL` 을 고치고 다시 배포하세요.
- 구글 개인계정은 하루 100통 한도라, 하루 30통까지만 메일을 보냅니다.
  **넘어도 의견은 계속 저장됩니다** — 관리자 화면에서 전부 보입니다.

## 수집 항목 (개인정보 최소화)

| 수집 | 안 함 |
|---|---|
| 접속 수, 조회 골프장 이름 | ❌ 위치 좌표 (신고 의무 발생 — 절대 금지) |
| 조회한 골프장의 **시/도 이름**(예: 경기) | ❌ 이용자가 있는 위치 |
| 사용 기능, 기기 종류(iOS/Android/PC), 앱 버전 | ❌ 이름·전화번호·이메일 |
| 연령대·성별 (동의자만) | ❌ 광고 식별자 |
| 베타 의견 (이용자가 직접 보낸 것만) | ❌ 회신용 연락처 |
| 무작위 기기 ID (개인 연결 불가, 순방문자 집계용) | |

개발용 접속(localhost)은 통계에 넣지 않습니다 — 우리가 미리보기로 여는 것이
이용자 통계에 섞이면 베타 100명의 진짜 사용 패턴이 묻히기 때문입니다.
