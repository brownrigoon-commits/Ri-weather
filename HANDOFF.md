# 골프라이프 작업 이어하기 가이드 (2026-07-27 v125 갱신)

집/회사 어디서든: **`python tools/sync.py`** 로 시작. 로컬 서버: `python -m http.server 8734`
권한 설정(승인 질문 없애기): `Github_코드백업/_클로드_설정/설정적용.py` 실행 후 클로드 재시작.

---

# 📈 Ri_Stock — 주식 앱 (2026-07-28 main 병합, **사장님 확인 필요**)

> 이 저장소에 앱이 하나 더 생겼습니다. 골프앱은 그대로 있고, 파일이 겹치지 않습니다.
> **main 에 합쳐졌습니다** — 폰 주소가 열리고, 깃허브 예약 실행도 이때부터 돕니다.

## 무엇을 만들었나

사장님이 손으로 돌리시던 두 프로그램을 **매일 저절로 도는 폰 앱**으로 바꿨습니다.

| 전 | 후 |
|---|---|
| `기업분석표_생성.py` 를 사람이 켜서 8분 대기 | 하루 두 번 자동 실행 (깃허브 서버 + 집 PC 이중) |
| `주식_대시보드/app.py` — PC에서만, 로그인 필요 | 주소 하나로 폰에서 바로 (설치·로그인 없음) |
| 대시보드가 엑셀을 뜯어 읽음 | 엔진이 JSON을 직접 만들어 앱이 그대로 읽음 |
| 어제와 뭐가 달라졌는지 모름 | 어제 대비 **신규 편입/이탈** 자동 표시 |

주소: `https://brownrigoon-commits.github.io/Ri-weather/ristock/`
(**main 에 합쳐진 뒤부터** 열립니다)

## 어디까지 됐나 — 검증된 것

| 검사 | 결과 |
|---|---|
| 점수·전략 계산이 예전과 **한 글자도 같은가** (정답지 재현) | 16/16 통과 |
| 파이썬 엔진 단위 검사 | 277개 통과 |
| 화면 로딩·버튼이 실제로 눌리는가 (헤드리스 크로미움) | 128개 통과 |
| 폰 크기(375×812) 전체 동선 | 297개 통과 |

**점수 체계와 전략 8종의 선정 결과는 예전 프로그램과 완전히 같습니다.** 이게 이 작업의 1번 조건이었습니다.

## ⚠️ 아직 확인 못 한 것 (정직하게)

여기 적힌 것은 **된다고 보고할 수 없는 항목**입니다. 사장님이 아래 3~4번을 하시면 그때 확정됩니다.

1. **실제 데이터 수집을 한 번도 못 돌려 봤습니다.** 작업 환경에서 야후·네이버·구글뉴스가
   전부 차단(403)돼 있었습니다. 지금 앱에 보이는 데이터는 **기존 엑셀에서 옮겨 심은 7/16 자료**입니다.
   → 진짜로 도는지는 **Actions 수동 실행 1회**(아래 2번)로 확인해야 합니다.
2. **GitHub Pages 에서 실제로 서빙되는지 확인 못 했습니다.** main 병합 전이라 주소가 아직 없습니다.
   → 병합 후 워크플로가 스스로 확인합니다(`공개 주소 도달 확인` 스텝). 빨간불이면 그게 신호입니다.
3. **아이폰 실기기에서 못 봤습니다.** 헤드리스 크로미움 375×812 로만 확인했습니다.
4. 윈도우 배치·PowerShell 스크립트도 **리눅스에서 만들었을 뿐 윈도우에서 못 돌려 봤습니다.**

## 사장님이 하실 일 — 이 순서대로

### 1) main 에 합치기 ✅ 완료 (2026-07-28)
가지 `claude/ri-stock-automation-cbi0pc` 를 main 에 합쳤습니다.
**깃허브의 예약 실행은 main 에 있는 것만 동작합니다.** 합치기 전에는 자동으로 아무 일도 일어나지 않았습니다.

### 2) Actions 에서 손으로 한 번 돌려 보기 (제일 중요)
`https://github.com/brownrigoon-commits/Ri-weather/actions`
→ 왼쪽 **"Ri_Stock 일일 자동 갱신"** → 오른쪽 **Run workflow** → 초록 버튼.
10~20분 걸립니다. **여기서 처음으로 진짜 수집이 돌아갑니다.**

- 초록 ✔ → 성공. 폰에서 새로고침하면 오늘 데이터가 보입니다.
- 빨간 ✘ → 실행 화면에 무엇이 걸렸는지 표로 나옵니다.
- **한국만 실패해도 앱은 멀쩡합니다.** 한국은 예전 데이터, 미국은 새 데이터로 보입니다.
  이때 저장소 **Issues 탭에 알림이 하나 열리고 메일이 옵니다.** 다시 채워지면 저절로 닫힙니다.
  (같은 문제로 매일 메일이 오지는 않습니다. 5회마다 한 번만 다시 알립니다)

### 3) 집 PC 예약 걸기 (5분, 한 번만)
깃허브 서버는 해외 IP라 네이버 금융이 한국 데이터를 막을 때가 있습니다. 집 PC가 그 보험입니다.
```
cd C:\Ri-weather
C:\Python314\python.exe -m pip install -r ristock\engine\requirements.txt
powershell -ExecutionPolicy Bypass -File tools\ristock_스케줄등록.ps1 -등록
powershell -ExecutionPolicy Bypass -File tools\ristock_스케줄등록.ps1 -상태
```
자세한 그림 설명은 **`docs/ristock_사용안내.md`** 에 있습니다.

### 4) 폰 홈 화면에 아이콘 만들기
아이폰은 **사파리**로 위 주소 → 공유 버튼 → "홈 화면에 추가".
안드로이드는 크롬 → ⋮ → "홈 화면에 추가".

## 자동 갱신 시각

| 한국시간 | 누가 | 담는 것 |
|---|---|---|
| 16:30 월~금 | 깃허브 서버 | 그날 한국 장 마감 |
| 16:40 매일 | 집 PC | 위와 같음 (보험) |
| 07:00 화~토 | 깃허브 서버 | 밤사이 미국 장 마감 |
| 07:10 매일 | 집 PC | 위와 같음 (보험) |

## 다음에 이어받는 사람에게

- **계약서부터 읽으세요**: `docs/ristock_설계.md`. 엔진·화면·자동화 세 파트의 약속입니다.
  필드 이름 하나만 어긋나도 화면이 빕니다.
- **점수·전략 결과를 바꾸면 안 됩니다.** 고치기 전후로 아래 4개를 반드시 돌리세요.
  ```
  python -m pytest ristock/engine/tests -q          → 277 passed
  node ristock/engine/tests/test_js_golden.mjs      → 정답지 16/16
  python3 -m http.server 8791                       (저장소 최상단에서 띄운 채로)
  node ristock/tests/verify_pwa.mjs                 → 149/149
  node ristock/tests/verify_phone_e2e.mjs           → 297/297
  ```
- **아이폰 앱 전환(2026-07-28)** — 홈 화면 아이콘으로 열 때의 완성도를 올렸습니다.
  - `ristock/icons/splash/` iOS 스플래시 8장 (253KB). 앱을 켜는 순간 흰 화면이 번쩍이던 것을 없앴습니다.
    아이폰은 **해상도가 정확히 맞는 것만** 씁니다 — 새 기종이 나오면 `make_icons.py` 의
    `스플래시목록` 에 한 줄, `index.html` 에 `<link>` 한 줄을 같이 추가해야 합니다.
    (둘 중 하나만 고치면 verify_pwa 가 잡습니다)
  - 브리핑 맨 위 **홈 화면 추가 안내 카드** — 아이폰 사파리에는 설치 버튼도
    `beforeinstallprompt` 도 없어서 앱이 직접 경로를 알려 줍니다.
    두 번째 방문부터 뜨고, ✕ 로 닫으면 다시 안 뜹니다(`ristock.설치안내.v1`).
    판단은 전부 `RiData.설치안내보일까()` 안에 있습니다.
  - 아이콘은 `any`(icon-512)와 `maskable`(icon-512-maskable)을 **분리**했습니다.
    겸용으로 두면 안드로이드 런처가 원형으로 자를 때 여백이 모자랍니다.
  - manifest `screenshots` 는 넣지 않았습니다 — **iOS 가 읽지 않는 필드**입니다.
    안드로이드 설치 시트를 꾸밀 때 그때 넣으면 됩니다.
  - 안드로이드 APK(TWA)는 **설계만 해 두고 만들지 않았습니다** → `docs/ristock_앱전환_설계.md` §3.
- `ristock/data/` 는 **자동 생성물입니다.** 손으로 고치지 마세요. 충돌 나면 재생성이 답입니다.
- **`ristock/data/archive/` 는 이제 커밋하지 않습니다**(2026-07-27, `.gitignore`).
  회차마다 0.6MB 씩 커밋되어 연 40MB 씩 저장소가 무거워지고 있었습니다
  (git 이력은 지워도 남습니다 → 집·회사 두 PC 의 clone·pull 이 그만큼 느려집니다).
  archive 는 내 PC 확인용 로컬 사본으로만 남고, '어제 대비 변화'는
  `ristock/data/picks_baseline.json`(전략이 뽑은 티커 목록만, 약 20KB)로 계산합니다(설계서 2.6).
  ⚠ 이 방식으로 바꾼 **첫날 하루는** 기준선이 없어 `changes.json` 이 나오지 않습니다.
  그날 아침·저녁 **두 회차 모두** '어제 대비 변화' 카드가 안 뜹니다
  (기준선은 날짜가 바뀌어야 비교 기준으로 올라가기 때문입니다 — 설계서 2.6).
  **다음 날 첫 회차부터** 정상으로 뜹니다.
- 전략 계산은 **JS 가 정본**(`ristock/js/strategy.js`), 파이썬은 같은 규칙의 복제본입니다.
  한쪽만 고치면 두 결과가 갈라집니다.
- 남은 일 후보: ① 실제 수집 1회 성공 확인 ② 아이폰 실기기 확인
  ③ 네이버 차단 시 한국 재무·수급 대체 경로 ④ 관심종목 백업(지금은 폰 안에만 저장)

---

## 🟢 로컬 PC 에서 이어받으실 때 — 여기부터 (2026-07-27, v125)

```
python tools/sync.py                    ← 클라우드에서 한 작업 받기 (v125)
python tools/verify_deploy.py --wait    ← ★ 가장 먼저 이것부터
```

**클로드에게는 `docs/로컬_클로드에게.md` 를 읽으라고 하세요.** 전부 거기 적어뒀습니다.

### ⚠️ v125 는 배포됐지만 검증이 반쪽입니다

클라우드 세션은 `github.io` 접속이 막혀 있어(`CONNECT tunnel failed, 403`)
`verify_deploy.py` 를 돌리지 못했습니다. 확인한 것은 여기까지입니다:

| 확인 | 결과 |
|---|---|
| 무결성(홀·이미지) | 4,239홀 · 이미지 전부 존재 |
| 필수 파일이 저장소에 | 30/30 |
| GitHub Pages 빌드 | 성공 |
| 원격 저장소 파일 수신 | 21/21 |
| APP_VER ↔ sw.js 캐시 | v125 ↔ v125 일치 |
| 클럽 피팅 자동 검사 | 4개 클럽 통과, 오류 0 |

**서버가 사용자에게 실제로 내보내는지는 확인 못 했습니다.** 로컬에서 한 번 돌려주세요.

### v125 에 들어간 것 — 클럽 피팅 전면 재설계

| 영역 | 내용 |
|---|---|
| **흐름** | 드라이버 강제 시작 폐지 → **첫 화면에서 클럽 선택**. 신체·실력은 공통 프로필로 1회만(재방문 25→16문항) |
| **드라이버** | 개인 경험 기반 로직을 **전문 근거로 재작성**. 로프트 계산 신설, 출발×휨 9조합으로 궤도·페이스 분리 판정, 티 높이·타점·캐리 편차 |
| **아이언** | 26문항. 볼자국·디봇으로 라이각, 어드레스 취향으로 헤드 타입, 긴 아이언 빼라는 조언 |
| **웨지·퍼터** | 잔디 상태·벙커 자신감 → 바운스 / 그린 빠르기 → 헤드 무게, 손 위치 → 로프트, 눈 위치 → 길이 |
| **그립** | 독립 문항. 손 길이·장갑 호수 사이즈표 + 땀·관절·테이퍼 규칙 |
| **결과 설명** | 진단 → 추천 → **왜 이 조합인지** → 기대 → 한계. 드라이버 약 3,300자 |
| **현행/단종 2단** | 1차 최신 모델, 2차 단종 포함 최적안 + 가격대 표시 |
| **아이콘** | 사장님 제공 실루엣 4종 (드라이버·아이언·웨지·퍼터) |

### 새로 생긴 파일
```
tools/clubfit_check.js       피팅 자동 검사 (4클럽 전 흐름)
tools/build_club_icons.js    아이콘 생성 (assets/src 원본 → 마스크용 PNG)
tools/build_clubfit_demo.js  피팅 화면 한 장짜리 미리보기
tools/collect_club_specs.py  제조사 스펙 수집기 ← 다음 작업
tools/verify_deploy_raw.py   보조 검증 (verify_deploy.py 대용 아님)
assets/src/                  아이콘 원본
docs/클럽피팅_질문설계.md      질문 설계 근거
docs/로컬_클로드에게.md        ★ 이어받기 상세
```

### 다음 작업
1. `verify_deploy.py --wait` 통과시키기
2. **제조사 스펙 수집** — 지금 스펙은 **전부 미검증 데모 샘플**입니다.
   질문을 26개로 늘려도 고를 물건이 12개면 결론이 같은 데로 수렴합니다. 최대 병목.

---

## 🆕 7/27 (집 PC) — 골프라이프 리브랜딩 + 토스뱅크 전면 개편 (v115 배포·검증 완료)

**앱 이름이 Ri-Weather → 골프라이프(Golf Life)로 바뀌었습니다.** 저장소·URL은 그대로(Ri-weather).
`js/legal.js` 의 서비스명은 "골프라이프(Golf Life, 구 Ri-Weather)" 로 병기 — 같은 서비스이므로
LEGAL_VERSION 은 올리지 않았습니다(올리면 전 사용자 재동의가 뜸).

### 디자인 — 토스뱅크 벤치마킹
다크 글래스 → **라이트 테마**로 전면 반전. 이전 다크 테마는 git v110 에 있습니다.
- 배경 #f2f4f6 / 카드 #fff / 글자 3단(#191f28·#4e5968·#8b95a1), 자간 -0.02em
- **색 역할 분리** (이게 핵심 — 함부로 합치지 말 것):
  · `--brand #10c43c` 로고 그린 — 진행바·활성 표시·테두리
  · `--primary #0b9e36` 흰 글씨가 올라가는 면(버튼) — 밝은 로고 그린은 흰 글씨 대비가 2.3:1 뿐이라 못 씀
  · `--primary-ink #0a7f2c` 흰 배경 위 그린 글씨
  · `--flag #f5232b` 로고 깃발 빨강(서브) — 핀/지도 마커/오버파/즐겨찾기 별에만
  · `--rain #3d8bfd` 강수 **데이터 색** — 브랜드색과 별개. 비를 초록으로 칠하면 안 읽힘
- **채운 버튼은 화면당 하나만.** `.guide-close` 같은 보조 동작을 채우면 안 됨(한 번 사고 났음)
- 화면 전환 fade+slide(`.is-entering`/`.is-entering-back`), 눌림 scale(0.97), 리스트 순차 등장(`.stagger`)
- 지도 타일 dark_nolabels → **light_nolabels**
- `word-break: keep-all` — 한글이 단어 중간에서 끊기던 문제

### 대기 화면 (`js/loading.js`)
스피너 대신 **말을 건다.** `WAIT.open(종류)` / `WAIT.run(종류, fn)`.
상황별 문구가 1.9초마다 바뀌고 진행바는 92%까지만(끝난 척 금지).
클럽 피팅은 **피팅샵 서사** — "피팅샵 문을 열고 들어갔어요 → 강도와 무게를 확인하는 중 →
샤프트를 깎고 있어요 → 그립을 감고 있어요". 사장님이 원한 토스 감각의 핵심입니다.

### 클럽 피팅 확장
드라이버 결과 → **내 백 허브** → 아이언/웨지/퍼터 각 3문항. 하나만 해도 저장됨.
- **선호 브랜드 1순위 + 다른 브랜드 대안**("이 샤프트도 골퍼님께 잘 맞을 것 같아요")
- ⚠️ `pickByBrand()` 의 안전장치: 브랜드 선호가 **필수 조건(소재·무게)을 덮어쓰지 못하게** 함.
  (후지쿠라 선호 → 스틸 107g 목표인데 그라파이트 88g 를 1순위로 올린 버그가 있었음)
- 진행 표시는 `NARRATION` 테이블 — "가봉/본봉" 재단 용어는 골퍼에게 의미가 없어 전부 교체
- 내 백 → AI 캐디 주입: 드라이버·아이언 + **웨지 로프트 구성**(가진 클럽만 지정하도록)

### 검증 방법
`window.__cfTest(입력[, "iron"|"wedge"|"putter"])` 로 엔진 직접 실행.
브라우저 콘솔에서 **400조합 랜덤 스윕**을 돌려 밴드 역전·로프트 순서·undefined 노출을 자동 검사했습니다
(스윕 스크립트는 임시 파일이라 저장소에 없음 — 필요하면 재작성).

**남은 것**: 로고 확정(제작 중), 폰트(Pretendard 웹폰트 미내장 — 굵기·자간으로만 맞춤),
클럽 스펙 실측 검증(아이언·웨지·퍼터도 드라이버와 동일하게 미검증).

### 7/27 새벽 (v124까지) — 숙박 메뉴 · 캔버스 하늘 · 백엔드 복구

**🔴 백엔드 백업·복구가 배포된 적이 없었다** (사장님 기록 손실 사고).
Code.gs 에 코드는 v109 때 커밋됐지만 **Apps Script 프로젝트를 재배포하지 않아**
버전 7(7/23)이 그대로 떠 있었다. → 버전 8로 재배포 완료, 저장→삭제→복구 전 과정 확인.
· 교훈: Apps Script 는 저장만으로 반영되지 않는다. **배포 관리 → 기존 배포 수정 → 새 버전**
  (새 배포를 만들면 URL 이 바뀌어 앱이 끊긴다)
· 앱도 고쳤다: 첫 백업이 **실제 저장돼야** '켜짐'이라고 표시. 실패하면 빨간 경고.

**하늘 효과는 캔버스로** (`js/weatherfx.js`). CSS 타일·요소 방식은 두 번 다 격자무늬로 보였다.
`WXFX.scan(부모)` 한 번이면 되고, `WXFX._frame(dt)` 로 rAF 없이 한 프레임을 강제할 수 있다(검사용).
밀도는 `scratchpad/render_rain.py` 로 **이미지를 뽑아 눈으로 맞춘 값**이다.
· ⚠️ `wx-cloud` 는 '흐림 상태'(카드·장면), 구름 덩어리는 `wx-puff`. 이름을 합치면 카드 전체가 블러된다.

**선호 브랜드 우선 추천** — 사장님 확정 원칙: 선호가 있으면 **무조건 그 브랜드 안에서 1순위**.
· 브랜드 질문을 선택 단계에 두었더니 대부분 질문을 못 받고 기능이 죽어 있었다 → **기본 문항(가봉 7)으로 이동**
· 점수 가드(전체 1위의 70%)가 선호 브랜드를 통째로 탈락시켰다 → **제거**. 하드 조건(소재·무게)만 남김
· 아이언 샤프트는 계열이 다르다(니폰·트루템퍼·KBS) → 클럽별로 브랜드를 따로 묻는다

**숙박** (`js/stay.js`) — 카카오 AD5, **골프장 좌표 기준** 10km. 추천순/가까운순 + 종류 필터.
평점·사진은 맛집과 같은 백엔드. **요금은 넣지 않는다** — 공개 API 가 없어 확인 불가.
대신 야놀자·여기어때·트립닷컴 검색 링크(응답코드 200 확인분만).

**검사 도구가 `tools/sweep.js` 로 들어왔다.** 브라우저 콘솔에서 실행한다.
지금 잡는 것: 스크립트 실행·문법 / 백업·복구 실동작 / 하늘 카드 16조합 / 하늘 효과 픽셀 /
브랜드 우선 36조합 / 브랜드 질문 위치 / 예약 링크 / 피팅 엔진 400조합 / 재단 용어 / 화면 undefined.
**검사는 반드시 자가검증할 것** — 일부러 고장을 내서 잡히는지 확인하지 않으면 통과가 무의미하다
(실제로 app.js 가 통째로 죽었는데 스윕이 통과한 적 있음).

### 7/27 밤 추가분 (v119까지)

**앱 이름 = 골프라이프.** 아이폰 날씨 앱처럼 카드가 그날 하늘이 된다:
`wxScene(code, is_day)` 가 조건별 배경 + 움직임(비·눈·구름·번개·안개)을 만들고
`wmoClass()` 가 하늘 종류를 정한다. 홈 카드 + 상세 히어로에 적용.
· ⚠️ 목록 카드에서는 해·달을 숨긴다(온도 숫자와 겹침) — `.course-card .wx-sun/.wx-moon { display:none }`
· 🐛 고친 것: 소나기(80~82)가 눈으로 분류되던 기존 버그. 눈은 71~77·85·86 만.

**클럽 피팅 진행 = 티샷.** 막대 대신 골퍼가 공을 쳐서 그린에 홀인한다.
캐릭터는 사장님이 주신 실루엣(`assets/golfer.png`, 배경 투명·지면선 제거)을
CSS 마스크로 써서 색을 입힌다 → 테마 색이 바뀌어도 따라온다.

**용어**: '가봉/본봉'은 골퍼가 모르는 재단 용어라 화면에서 전부 제거.
`NARRATION`(질문별 이야기) + `STAGE_LABEL`(내부 단계명 번역) 두 겹으로 막는다.
스윕에 재단 용어 누출 검사가 들어있으니 새 문항을 추가하면 자동으로 걸린다.

**대기 화면**: 원형 % 링 + 단계 문구. `w.say(문구, 퍼센트)` 로 실제 진행에 맞춰 몰 수 있다.
맛집은 거리순 먼저 보여주고 재정렬하던 방식을 버리고 **찾기→평가→사진까지 끝내고 한 번에** 표시.
· 사진은 상위 12곳만 미리 받는다(전체는 40초 넘음). 나머지는 스크롤 시 채워짐.

**🐛 라이트 테마 전환 때 놓쳤던 것들** (같은 실수 반복 금지):
· 보조 버튼(`.guide-close`)까지 초록으로 채워 '채운 버튼 2개'가 됨 → 화면당 하나만
· 플로팅 버튼이 어두운 배경 + 어두운 아이콘 → 안 보임. 흰 원 + 진한 아이콘으로
· 비 예보가 브랜드 초록으로 칠해짐 → 강수는 `--rain` 파랑(데이터 색은 브랜드색과 분리)

**배포 검증 보강**: `release_courses.py` 가 이제 **CSS 가 참조하는 파일**(url(...))도 확인한다.
js/legal.js 누락 사고와 같은 구멍이 이미지에도 있었음.

---

## 🆕 7/26 (집 PC) — 클럽 피팅 메뉴 추가 (v110 배포·검증 완료)

허브 5번째 타일 **🎯 클럽 피팅**. 설계 원칙(외부 인수인계 문서 기준, 사장님 확정):
- **룰 엔진이 후보를 압축하고 AI는 설명만** — 스펙 수치는 절대 생성하지 않음
- **"그대로 쓰세요" 유지(KEEP) 판정 존재** — 항상 교체를 권하지 않음(신뢰 장치)
- 이미 아는 정보(연령·성별·평균타수·후반패턴)는 **다시 묻지 않음** (동의화면+스코어 기록에서 자동)
- 후반 체력 보정: 36홀 응답 또는 스코어 기록의 후반 +3타 패턴 → 무게 −8g·플렉스 한 단계 ↑
- 결과는 **"내 백"** `localStorage riweather.mybag` 저장 → AI 캐디 playerTraits/playerTraitGuide 에 주입
  (드라이버 캐리·7번 캐리로 티샷/세컨드 클럽 지정 조언)

파일: `js/clubfit.js`(엔진+화면 전부), index.html `#clubfit-view`(페이퍼 무드), style.css `.cf-*` 스코프, sw.js CORE.
검수 6케이스는 콘솔에서 `__cfTest({...})` 로 재현 가능(케이스1 keep=true, 케이스2 shaft1=벤투스 TR 블랙 5X 확인 완료).
**주의**: 샤프트/헤드 스펙은 데모 샘플 — 실측 검증 전. 스펙 문구를 사용자에게 단정적으로 노출하는 확장은 보류.

---

## 🔴 오늘(7/22 오후) 회사에서 일어난 일 — 반드시 읽을 것

**배포가 사용자에게 도달하지 않는 사고가 두 건 있었습니다.** 원인과 조치:

1. **`.nojekyll` 누락** → GitHub Pages 빌드가 조용히 실패(882MB 저장소를 매번 Jekyll 변환 시도).
   v81·v83·v84 배포가 사용자에게 도달하지 않음. → `.nojekyll` 추가로 해결.
2. **`js/legal.js` 가 배포 목록에서 누락** → 서버에서 404 HTML 이 서빙되어
   약관 관련 버튼(보기·시작하기·처리방침)이 전부 죽음. "나중에 하기"만 살아있어 원인 파악이 늦었음.
   → `release_courses.py` 가 **폴더 통째로** add 하도록 변경.

**그래서 생긴 규칙 (CLAUDE.md 에도 있음):**
```
python tools/release_courses.py "메시지"
python tools/verify_deploy.py --wait      ← 이걸 통과해야 '배포 완료'
```
`verify_deploy.py` 는 실제 서버에서 파일을 받아 404·버전·빌드상태를 확인합니다.
**로컬에서 되는 것과 사용자에게 도달하는 것은 다른 문제입니다.**

또 하나: 화면 검증은 `버튼.click()` 이 아니라 **`document.elementFromPoint`** 로 실제 터치 지점을 확인할 것.
(덮여 있거나 화면 밖으로 나간 버튼을 `.click()` 은 잡지 못함 — 실제로 이것 때문에 놓쳤음)

**진단 도구가 앱에 내장됨(v89, v106 개선):**
- v106: iOS가 만들어내는 '내용 없는 error 이벤트'(메시지·파일·줄번호 전부 없음)는 표시하지 않음
  — 이걸 안 걸러서 빨간 배너가 화면을 덮은 사고(7/24)가 있었음. 같은 오류 반복은 (×N)으로 합침, 최대 8줄.
- 파일 누락·JS 오류가 나면 **폰 화면 상단에 빨간 띠**로 표시된다. 스크린샷 한 장이면 원인 확정.
- 주소 뒤 `?diag` → 버전·기기·UA·화면높이 표시 (사용자 문의 대응용)
- 주소 뒤 `?reset` → 캐시·서비스워커·저장값 전부 삭제 후 최신으로 재시작

**인앱 브라우저 조사 결과 중 반증된 것(다시 삽질하지 말 것):**
- `backdrop-filter` 가 터치를 막는다 → **근거 없음**(오진이었음). style.css 주석은 무시할 것
- `disabled` 속성 → 이미 제거됨. `.consent-start:disabled` 규칙은 죽은 코드
- iOS 는 click 을 합성하지 않는다 → 부정확. 직접 등록한 리스너는 항상 동작함

---

## 현재 상태 (v88 배포·검증 완료)

**등록 완료 233구장 4,257홀** — 전부 홀 완전체 + 이미지 존재 검증 통과 (감사 불합격 0)

| 항목 | 수치 |
|---|---|
| 등록 구장 | 233 (국내 골프장 635곳 중) |
| 등록 홀 | 4,257 |
| 홀맵 이미지 | 4,257 (100%) |
| 골프존 3D 영상 | 3,879 |
| AI 캐디용 영상 프레임 | 3,852홀 × 3컷 |

주력 공급원은 **골프존**(218클럽 전량 등록 완료 = 소진). 나머지는 각 골프장 공식 홈페이지 파싱분.

**맛집 기능**: 카카오 로컬 API 연동 완료 (자유로CC 2곳 → 64곳). 키는 app.js `EMBED_KAKAO_B64`.
카카오 개발자 앱 = "Ri-Weather"(ID 1520230), 카카오맵 API 활성화됨(무료 쿼터 이 앱에 귀속).

## 절대 원칙 (사장님 확정)

1. **홀이 하나라도 빠지면 등록 금지** — 공식 홀 수(골프존) = 파싱 홀 수 일치 필수
2. **틀릴 수 있으면 아예 표시하지 않음** — 거리·코스명이 의심스러우면 그 항목만 제거하고 등록
   · 위성사진 기반 홀 트레이싱, 그린 경사 추정은 **전량 폐기**(`js/holesdb.js` 비움)
3. 홀맵 이미지 표준: 지도만, 흰배경, 세로 600px 고정·가로≤680 (`tools/crop_map_only.py`)
4. 미등록 구장은 앱이 **위성 전경 + "홀별 공략 준비 중"** 배너 자동 표시
5. 사용량 절약: 단계별 확인 왕복 금지 — 일괄 실행 + 자동 검증 + 최종 요약

## 골프존 파이프라인 (주력)

```
tools/golfzon_collect.py        골프존 로비 API로 클럽·홀 JSON 수집 → coursedata/golfzon/cc_*.json
tools/gz_link_kakao.py          골프존 클럽명 ↔ golfdb.js 연결 (카카오 주소/POI 검증)  [--write]
tools/golfzon_build.py          골프존 JSON → parsed.json (홀맵 이미지 크롭 + 영상 URL) [--write]
tools/extract_video_frames.py   3D 영상에서 3컷 추출 → parsed.json 에 frames 기록
```

**골프존 JSON 구조 주의**: 홀 목록은 `holeInfo.holeInfoList` 가 **코스별 2차원 배열**이다.
(`len(holeInfoList)` = 코스 수, 홀 수는 `sum(len(c) for c in holeInfoList)`)

**클럽명 매칭이 핵심 난관**. 과거 37곳이 데이터를 갖고도 누락됐던 원인:
- `golfzon_build.load_golfdb()` 가 별칭 필드 `a`를 안 읽었음 → `coursedata/gz_alias.json` 참조로 해결
- Nominatim이 한국 지번주소(`산 39-1`)를 못 찾음 → **카카오 로컬 API**로 교체(실패 0건)

**연결 판정 규칙** (오연결 방지, `gz_link_kakao.py`):
1. `CONFIRMED` 표 — 카카오 주소가 완전히 같아 사람이 확인한 개명 구장 (H1 CLUB=에이치원클럽,
   포웰CC 안성=루나힐스안성CC, 청우GC=알프스대영CC, 한림용인CC=레이크힐스용인CC, 화순엘리체CC=엘리체CC)
2. 정규화 이름 완전일치 → 지오코딩 없이 연결 (오지오코딩 방지)
3. 좌표 2.5km 이내 + 이름 유사 후보가 **정확히 1곳**일 때만 연결
4. `FORCE_NEW` — 이름은 비슷해도 주소가 다른 별개 구장은 신규 항목으로 추가
   (골프존카운티 구미≠구미CC, 골프존카운티 순천≠순천CC, 해비치 제주≠해비치CC 등)

**같은 구장이 앱DB에 2항목**인 경우 `build_holeimgdb.py` 의 `MIRROR` 로 키 복제
(예: 샤인빌파크 PALM/RIVER → 어느 쪽으로 찾아도 같은 데이터).

## 홈페이지 파이프라인 (보조)

```
tools/analyze_registrable.py    수집 자산 → 등급 A~E 판정 (registrable_analysis.json)
tools/universal_build.py        사이트 유형 자동판별 파싱 → 검증 → 등록
      --club "타이거CC" --db "타이거CC" --slug tiger [--write]
      --batch --grades ABCD --write        (일괄, 이어하기 자동)
tools/cleanup_registrations.py  중복·코스명불량·거리이상 자동 정리
tools/audit_registered.py       품질 감사 (파합계·이미지·TIP·거리 상식)  [--fix 시 불합격 삭제]
tools/match_dbnames.py          등록 구장명 ↔ golfdb.js 표기 일치 교정
tools/build_holeimgdb.py        parsed.json → js/holeimgdb.js 조립
tools/release_courses.py "메시지"  조립+무결성+버전업+push 원클릭 배포
tools/export_status_excel.py    골프장DB.xlsx 현황 분류표 생성
```

**지원 사이트 유형** (universal_build.py): `holebox`(타이거형) · `tabpane`(감곡형) · `holeinfo`(몽베르형)
· `generic`/`generic-loose`(홀번호 이미지 + 주변 문맥 해석) · `perhole`(홀별 개별 페이지).
페이지마다 전 파서를 시도해 **연속 홀 세트를 만드는 최선**을 채택.

**검증 게이트** (하나라도 실패 시 등록 안 함): 홀번호 연속 · 공식 홀수 일치 · 9의 배수 · 이미지 존재
· 이미지 세로형 · 9홀 파합계 33~39(파3코스 예외) · 동일 도메인 중복 등록 방지

## AI 캐디

`js/app.js` 가 홀맵 이미지 + **골프존 3D 영상 프레임 3컷**을 함께 Gemini에 보내 공략 생성.
모델 `gemini-flash-latest`, 실패 시 `gemini-flash-lite-latest`. 영상은 AI 캐디 **아래쪽**에 별도 배치.

## 골프장DB.xlsx (현황 분류표)

시트: 한국 전체(635) / 등록완료(233) / 제작가능(0) / 부분수집(25) / 자료부족(181) / 자료없음(197) / 일본 / 중국
상태별 색상 구분, 각 행에 홀이미지 수·공식 홀수·실패 사유·홈페이지 URL 기록.
`python tools/export_status_excel.py` 로 언제든 갱신 (파일 열려 있으면 골프장DB_현황.xlsx로 저장).

## 7/22 회사에서 새로 만든 기능 (전부 배포·검증 완료)

| 기능 | 파일 | 비고 |
|---|---|---|
| **집·회사 동시작업 동기화** | `tools/sync.py` | 충돌 자동해결. 13개 시나리오 검증 통과 |
| **배포 검증** | `tools/verify_deploy.py` | 실제 서버에서 404·버전·빌드 확인 |
| **홈 화면 추가 버튼** | `#install-cta`, `#guide-sheet` | 아이폰/안드로이드/인앱 자동 감지. 설치하면 자동으로 사라짐 |
| **이용약관 동의 화면** | `#consent-view`, `js/legal.js` | 14세·약관 필수 / 위치·연령대성별·마케팅 선택 |
| **약관 미동의 안내** | `#nag-sheet`, `CONSENT_NAG` | '나중에' 누르면 화면이동 5회마다 재안내 |
| **AI 캐디 개인화** | `playerTraits()`, `playerTraitGuide()` | 연령대·성별·**구력**을 실제 공략에 반영 |
| **업데이트 알림** | `#update-toast`, `APP_NOTE` | 버전 오르면 무엇이 바뀌었는지 4.5초 표시(배포 메시지 자동 반영) |
| **캐시 강제 초기화** | `?reset` | 주소 뒤에 붙이면 캐시·SW·저장값 삭제 후 최신으로 |

### 약관 관련 법적 근거 (조사 완료 — 변경 시 주의)
- **위치 좌표를 서버로 보내지 않는 한 위치기반서비스사업 신고 의무 없음**
  (방통위 해설서 명문). 좌표를 서버 저장하면 신고 필요 + 미신고 시 3년 이하 징역/3천만원 이하 벌금.
  → **절대 좌표를 서버로 보내지 말 것.** 통계가 필요하면 '조회한 골프장 ID'만 보낼 것.
- **선택 항목 미동의를 이유로 서비스 전체를 막으면 과태료 3천만원**(개인정보보호법 제16조③).
  그래서 위치·연령대·성별은 전부 **선택**이고, 미동의해도 모든 기능이 동작해야 함.
- 선택 항목 **기본 체크 금지**(다크패턴), 면책 조항에 **"고의 또는 중대한 과실이 없는 한"** 단서 필수.
- 회사 정보 미기입 상태: `js/legal.js` 의 `COMPANY` 에 사업자등록번호·주소·전화 넣어야 함(사장님 확인 필요).




## 2026-07-24 집 PC 작업 요약 (v105 배포됨)

**백엔드(Apps Script) 가동** — 사장님 계정에 '골프라이프 백엔드' 설치·배포 완료 (버전 7).
- URL은 js/stats.js `RIW_BACKEND` 에 있음. 시트 '골프라이프 통계'(log)로 이용통계 수집 중.
- 기능: doPost(통계 수집) / fn=placephotos(카카오 사진탭, 음식→메뉴→실내→실외 순) /
  fn=placemeta(평점·리뷰수 일괄) / fn=summary&pw=(관리자 요약, 비번 Code.gs ADMIN_PW)
- 코드 수정 시: tools/apps_script/Code.gs 수정 → script.google.com에서 붙여넣기 → 저장 →
  배포 관리 → ✏️ → 새 버전 → 배포 (URL 불변). **사진 소스는 반드시 '사진탭(tab/photos)' 유지** —
  이미지 검색·블로그 썸네일은 다른 가게 사진이 섞였던 사고 원인.

**맛집 완성** — 추천순(카카오 평점, 가게ID 정확) 기본 + 거리순/종류 칩 + 전면 펼침 카드(v2)
+ 사진 위에서부터 lazy 로딩 + 네이버 검증 링크. 예전 접이식은 app.js `FOOD_UI_V2=false` 로 복귀 가능.
가격순은 데이터가 없어 미구현(추측 금지).

**레이더 교체** — RainViewer가 무료 예측 중단(과거만 제공)해서, 한국 구장은
기상청 공식 '실황+2시간 예측' GIF(`weather.go.kr/w/repositary/image/rdr/img/qpr_{tm10분}.gif`)로 교체.
내 골프장 확대(2.4x)+빨간 점: 위경도→픽셀 아핀 보정(제주·울릉·독도·백령 4점, 잔차 ±2px) — app.js kmaPx().
상단 시각 띠 = 같은 GIF의 제목부(0,0~340,26)를 잘라 확대(동일 URL GIF는 프레임 동기 재생).
해외 구장은 기존 RainViewer 유지.

**로고 논의 중** — 이름 '골프라이프' 확정 분위기. naming.html(로컬 시안 페이지)에 마지막 후보
('P 문법의 G' 4안) 있음. 미확정.

## 7/24 회사 PC — 멈춤·오류 전수 감사 반영 (v107~v108, 전부 실서버 검증됨)

- **무한 로딩 제거**: `fetchT(url, opts, ms)` 도입(app.js 2350대). 날씨·검색·레이더·카카오·백엔드·사진·경로·AI·타일·프레임 **16곳 전부 시간제한**. 새 fetch 는 반드시 fetchT 사용.
- **회색 화면(스크롤해야 복구) 원인 3종 수정**:
  ① 평점 늦게 도착 시 스크롤 중 목록 재정렬 → 상단(scrollY≤200)일 때만 즉시 재정렬, 스크롤 중엔 배너를 '⭐ 추천순으로 보기' 버튼으로 전환(.food-reco-banner 는 sticky)
  ② body 의 background-attachment:fixed → body::before 고정 레이어로 분리 (iOS 타일 미갱신 버그)
  ③ .float-btn 의 backdrop-filter 제거(스냅샷 미갱신) — 배경 불투명도로 대체
- **지도 라이브러리 내장**: unpkg CDN → js/vendor/leaflet.js + css/leaflet.css (SW CORE 포함).
  설치형 PWA 오프라인에서 L 미정의 → 빨간 배너 연쇄가 원인이었음. openDetail/openCourseView 에 typeof L 가드.
- **오류 표시기**: 빈 이벤트 억제 조건에서 lineno 제외(빈 ErrorEvent 는 lineno=0 이라 v107 조건이 뚫렸음).
- **맛집 평점**: 백엔드 null 응답 검증(캐시 오염 방지) + Number() 강제(문자열 "4.5" → toFixed 크래시 방지) + .then 체인 .catch.
- 사용자 폰이 구버전이면: 홈 배지 APP_VER 확인 → `?reset` 안내.

## 다음 작업

0. **관리자 모드 + 통계 백엔드** ← 사장님이 요청한 다음 순서
   - 설계 방향 확정: Google Apps Script + 스프레드시트(무료), **좌표는 절대 수집 안 함**
   - 수집: 접속수, 조회한 골프장, 사용 기능, 기기종류, 연령대·성별(동의자만)
   - 관리자 화면: 숨은 주소 + 비밀번호. 일별 추이·인기 구장·성별연령 분포
   - ⚠️ 사장님이 직접 해야 하는 준비: 스프레드시트 생성 + Apps Script 배포(약 5분, 안내 필요)
1. **홈 화면 추가 안내 개선** — 고령 이용자가 직접 하기 어려워함.
   iOS는 프로그래매틱 설치가 **불가능**(애플 제약)하므로 안내 UX로 해결해야 함(화살표·그림 등)
2. **부분수집 25곳** — 홀 이미지를 이미 받아놨으므로 사이트별 전용 파서만 붙이면 등록 가능.
   가장 현실적인 다음 타깃. `registrable_analysis.json` 에서 `hole_imgs>=3` 인 항목 참조.
2. **자료부족 181곳** — 수집기 이미지 상한(30장) 상향 후 재수집·재분석
3. **자료없음 197곳** — 홈페이지에 홀맵을 안 올리는 곳이 다수. 추정 금지(원칙 2), 준비중 유지
4. 프레임 추출 실패 27홀 재시도 (`extract_video_frames.py` 는 이어하기 지원)

## 참고

- Gemini API 키: extract_tips.py 등에 내장 (무료 한도, 429 시 재시도)
- 골프존 API: `https://lobby.golfzon.com/v1/dotcom/courses/course/{ciCode}/details/hole-info`
  홀맵 이미지 `https://o.gzcdn.net/images/cc{mapUrl}` · 3D영상 `https://mediathumbnail.golfzon.com/media/cc/hole3d/{videoMapUrl}.mp4`
  (영상은 CORS 미허용 → 브라우저 JS로 바이트 읽기 불가. 프레임은 ffmpeg로 서버에서 추출해 저장)
- 배포: `release_courses.py`가 APP_VER·sw.js 캐시 동기화까지 처리
- 회사 PC Python: `C:\Python314\python.exe` (selenium·pillow 설치됨)
- ⚠️ 이 PC의 Git Bash에는 `pkill`/`pgrep` 없음. 프로세스 종료는 PowerShell로:
  `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | ? {$_.CommandLine -like "*키워드*"} | % {Stop-Process -Id $_.ProcessId -Force}`
- 장시간 작업은 PowerShell `Start-Process ... -RedirectStandardOutput` 으로 백그라운드 실행
